apiVersion: v1
kind: Service
metadata:
    name: waheedahmeddockercomposenodejsmysql
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: waheedahmeddockercomposenodejsmysql
